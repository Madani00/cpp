#include "Base.hpp"

int main() {

    // A a;
    Base *base = NULL;
    
    base = base->generate();


    base->identify(base);


    // Base *test;
    // test->generate();
    // test->identify(test);


}